# Crane original and amendment: implementation boundary

Current inspected base: e2fee997. XBFUS was explicitly confirmed by the user;
do not request the entity again or substitute Mexican tax evidence.

The existing manifest source already loads up to 21 ordered case messages.
Do not rebuild that capability. The missing boundary is associating reviewed
original/amendment evidence to the same isolated case before assessment.

Current duplicate engine treats shared thread and sender domain as probable,
not exact. Intake then creates a held case. The duplicate resolution command
updates a candidate decision, not message/document case ownership. Therefore
calling two single-message ingests is not proof of a unified request bundle.

Required controlled association:

1. Pin organization, both source Gmail IDs and source hashes, target new case,
   original sender and thread. Exclude the historical Salzillo and existing
   single-form Crane canary. Reconcile prior receipts before any replay.
2. Persist original and amendment separately under that case with immutable
   raw originals and attachments; reuse intake persistence and source loaders.
   A thread match alone must never authorize attachment to an arbitrary case.
3. Preserve QF-168 as superseded evidence; reviewed QF-167 replaces it in the
   active requirement set. Other five forms survive the amendment.
4. Legacy DOC must remain preserved with an explicit conversion/manual-review
   requirement. No file execution or false safe/completed status.
5. Changes to the source set invalidate downstream review, draft and release
   identity. A repeated identical command must return the existing receipt;
   mismatched hashes or target must conflict.

Source IDs from the read-only inventory: relays 1a084a5eb2deb7db and
1a084a5e86c31a38; provider thread d420e2d4-1d42-4f65-a296-71f9bfd9a715.
Job 5611ec1f-9238-480b-8289-e9305f1c9f19 failed; job
c3f07cf7-05f9-4997-8407-52555103e415 pending. No retry performed here.

Positive exit: one isolated case exposes both source identities, all original
requirements plus the reviewed supersession, and a complete or explicitly
pending disposition for every file. A held duplicate or parsed single form
does not satisfy this exit.
