# P2-S2 Platform55 Operate evidence

## Decision

- Milestone: Platform55 Operate workspaces for source intake, source history, human review, and approved rates.
- Implementation SHA before evidence closure: `7a22fef58fabeec2ab025d90dba8f364e1df3e51`.
- Final candidate SHA: `60eb7f341a09f6d65f4344b8606a9779c339712c`.
- Evidence closure HEAD reviewed independently: `18955d06443d3532823da6725eda90041b15b2e8`.
- Base SHA: `4a74b2fea0fbee89d09c3e56603e50cb7591e2f1`.
- Local implementation verdict: GO for P2-S2 after the sixth detached independent review of the exact closure HEAD.
- P2 progress credited: 45%.
- Overall production-readiness score: 79.2%.
- External status: local only. Push, PR, preview, Ready, merge, deployment, and Supabase changes remain unperformed and separately authorized.

## Delivered slice

- `upload-center.html`, `upload-history.html`, `staging-review.html`, and `rateware.html` now use the shared Platform55 tenant shell.
- The four pages retain their existing authentication bootstrap, page scripts, control IDs, URL/query behavior, and API calls.
- `src/platform55-operate.css` owns only Operate workspace composition; the shared shell remains the sole owner of rail, topbar, search, mobile drawer, and global navigation.
- Page scripts publish descriptive state only through `updatePlatform55Shell()`. Unknown page-state keys and action IDs fail closed, dynamic text uses `textContent`, and the shell never executes page actions.
- Source files remain preserved. Interpretation continues to land in `rate_staging` as `pending_review`; production insertion still requires human approval.
- Staging selection, blocker, approval, retry, and cross-page selection behavior remain under the existing handlers and authorization checks.
- Rateware continues to present approved rates only; this migration added no staging update or approval path.

## Visual and interaction evidence

- Rejected first fixture: `output/qa-platform55-operate-preview.html`. It uses the real shell, tokens, and Operate CSS with local fixture data only; it performs no request or write and does not count as accepted visual evidence.
- Rejected screenshot set: `docs/release/evidence/platform55-s2-operate/` contains only 23 PNGs and is not SHA-scoped.
- All accepted states had exactly one active route, no horizontal document overflow, no console errors, and readable `Operate · Page` breadcrumbs.
- Mobile closed navigation reported `aria-hidden=true`, `inert=true`, and zero tabbable descendants. Opening moved focus to Close navigation; Escape reclosed the drawer and returned focus.
- Ctrl+K moved focus into global search; Escape restored the previously focused control.
- Search and Ask AI retained accessible names at the mobile breakpoint.
- The rejected first matrix was missing Rateware loaded at `390x844`; the accepted replacement contains all 24 required captures.
- The fixture manually reconstructs workspace markup and imports only the shared shell. It does not load the actual route HTML, authentication bootstrap, or page modules, so none of these PNGs count as actual-route visual evidence.
- Visual review found and corrected a real breadcrumb separator defect before closure; the regression now requires `Operate · Source Files` rather than concatenated labels.
- Replacement evidence is stored at `docs/platform55-evidence/p2-s2/60eb7f341a09f6d65f4344b8606a9779c339712c/` with 24 PNGs, per-capture SHA-256 hashes, and source Git blob IDs anchored to the immutable subject.
- The replacement server returns the actual route HTML and page modules byte-for-byte, while substituting only deterministic authentication and data-service boundaries. It rejects non-GET/HEAD requests with HTTP 405 and never calls an external service.
- Browser probes confirmed exact PNG and browser viewport dimensions, one active route, the actual page module declaration, and no document overflow for every capture. The matrix includes the previously missing Rateware loaded state at `390x844`.
- Upload Center's non-happy state is a real empty-submit validation on the actual page; the screenshot is scrolled to its visible error message and the manifest records the trigger.
- Actual-route review exposed and corrected three defects hidden by the synthetic fixture: wide tables escaping their panel, Upload Center failing to stack on mobile, and its desktop header/queue falling into a tall implicit grid row.
- The 24 accepted PNGs were captured with CDP `Page.captureScreenshot` at exactly `1440x900`, `1024x768`, and `390x844`; cropped app-surface captures were rejected and removed.
- Every capture waited for at least three stable layout samples and records a content-width ratio of at least `0.65`; a transient Rateware tablet capture that collapsed into roughly one fifth of the viewport was rejected and regenerated.
- Schema v4 records a target selector, visible marker, and at least `0.8` state intersection for every capture. All four mobile error states are visibly inside the viewport.
- The evidence server returns HTTP 204 for `/favicon.ico`. A fresh-browser CDP audit of all 24 route/state/viewport combinations recorded zero console errors, zero page/request failures, and zero HTTP responses at or above 400.

## Verification

- `npm run test:platform55:operate`: PASS.
- `npm run test:platform55-shell`: PASS.
- `node tests/rateware-stability.test.mjs`: PASS.
- `node tests/upload-center.test.mjs`: PASS.
- `node tests/interpret-upload-normalization.test.mjs`: PASS.
- Relevant JavaScript syntax checks: PASS.
- Browser geometry, mobile focus, inert navigation, keyboard search, overflow, and console probes: PASS.
- Full `npm test`: PASS on exact closure HEAD `18955d06443d3532823da6725eda90041b15b2e8`.
- Action Contract validation: PASS with 401 contract entries, 399 discovered surfaces, zero errors, and one pre-existing declaration-path warning.
- `npm audit --audit-level=low`: PASS with zero vulnerabilities.
- Release progress after recording the independent GO: General 79.2%, P2 45%.
- Final syntax and diff checks: PASS.
- First independent detached review: NO-GO because the original visual matrix was incomplete, synthetic, outside the required SHA-scoped directory, and not anchored to the reviewed final candidate.
- Subsequent detached reviews rejected and corrected source-hash drift, a collapsed Rateware tablet capture, an off-screen Staging Review error, duplicated compositor regions, and the unhandled favicon console error.
- Sixth detached independent review: GO on exact closure HEAD `18955d06443d3532823da6725eda90041b15b2e8`; 24/24 fresh Chrome audits, 24/24 manual visual inspections, all focused and full gates PASS, and zero external mutation.

## Boundaries

- No endpoint, Edge Function, migration, DDL, DML, secret, environment value, approval rule, upload, staging row, or production data changed.
- This milestone does not claim a Vercel preview, authenticated preview smoke, production deployment, production smoke, or monitoring.
- It creates no Supabase preview branch and changes no Supabase state.
- Formal status remains NO-GO for full Platform55 production completion until the remaining P2 sprints and independent review gates finish.
