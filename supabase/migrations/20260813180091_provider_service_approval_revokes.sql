revoke all on table public.provider_approval_requests from public, anon, authenticated, service_role;
revoke all on table public.provider_approval_events from public, anon, authenticated, service_role;
revoke all on table public.provider_approval_queue from public, anon, authenticated, service_role;
revoke all on sequence public.provider_approval_requests_approval_number_seq from public, anon, authenticated, service_role;
